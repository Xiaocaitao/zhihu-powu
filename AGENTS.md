# 项目协作约定

- 每条 Git commit message 都必须包含本次需求描述，说明改动解决了什么用户需求。
- commit message 使用简洁、可读的中英文均可；推荐格式：`<type>: <需求描述>`。
- 前端只负责采集输入、透传请求、解析 SSE/JSON 和展示返回；不得预设默认业务数据、固定回复或业务决策。
- 业务规则、Agent 编排、工具选择、提示词和结果协议放在后端分层实现，避免把后端逻辑散落到页面脚本。
